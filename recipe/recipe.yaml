schema_version: 1

context:
  name: neo4j
  version: "6.3.1"

package:
  name: neo4j-python-driver
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/neo4j-${{ version }}.tar.gz
  sha256: e75530ca0691d69916c56acdb7401df4ee9e1d5084cfcfb3ede796db38667dcd

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools ==84.0.0
  run:
    - python >=${{ python_min }}
    - pytz

tests:
  - python:
      imports:
        - neo4j
        - neo4j.graph
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: Neo4j Bolt driver for Python
  license: Apache-2.0 AND Python-2.0
  license_file:
    - LICENSE.APACHE2.txt
    - LICENSE.PYTHON.txt
    - LICENSE.txt
    - NOTICE.txt
  homepage: https://github.com/neo4j/neo4j-python-driver

extra:
  recipe-maintainers:
    - pvanheus
    - thebestnom
    - xylar
